import { requireUser } from '@/lib/auth'
import Link from 'next/link'
export default async function Dashboard(){const {supabase,user}=await requireUser();const {data:profile}=await supabase.from('profiles').select('username,role,coins,score').eq('id',user.id).single();const {data:games}=await supabase.from('games').select('id,title,description,status').eq('status','published').order('created_at',{ascending:false}).limit(20);return <main className="shell"><nav><Link href="/dashboard">EduVerse</Link><Link href="/creator">Create game</Link><Link href="/admin">Admin</Link></nav><section className="card"><h1>Welcome, {profile?.username??user.email}</h1><p>Role: {profile?.role}</p><p>Score: {profile?.score??0} · Coins: {profile?.coins??0}</p></section><section><h2>Published games</h2><div className="grid">{games?.map(game=><article className="card" key={game.id}><h3>{game.title}</h3><p>{game.description}</p><Link className="button" href={`/games/${game.id}`}>Play</Link></article>)}</div></section></main>}
