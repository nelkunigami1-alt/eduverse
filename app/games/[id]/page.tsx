import { requireUser } from '@/lib/auth'
export default async function Game({params}:{params:Promise<{id:string}>}){const {supabase}=await requireUser();const {id}=await params;const {data:game}=await supabase.from('games').select('title,description,code').eq('id',id).eq('status','published').single();if(!game)return <main className="shell"><h1>Game unavailable</h1></main>;return <main className="shell"><article className="card"><h1>{game.title}</h1><p>{game.description}</p><div className="game-frame"><p>Approved game runtime.</p></div></article></main>}
